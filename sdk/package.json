{
  "name": "@handoff/sdk",
  "version": "0.1.0",
  "description": "Consume a Handoff grant: scoped, revocable, just-in-time AI memory for agents.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "pnpm build"
  },
  "keywords": ["sui", "walrus", "memwal", "ai-agents", "memory", "handoff"],
  "license": "MIT",
  "dependencies": {
    "@noble/ed25519": "^2.3.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
